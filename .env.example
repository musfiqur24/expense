# Used by docker-compose.yml
NODE_ENV=production
PORT=5000
MONGO_URI=mongodb://mongo:27017/expense_tracker
# The frontend uses the same origin through NGINX, so * is suitable for this app.
# If the API will be accessed directly, set your public HTTPS URL instead.
CORS_ORIGIN=*
